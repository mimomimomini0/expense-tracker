# Copy to .env and fill in real values. Never commit .env.
ANTHROPIC_API_KEY=sk-ant-...
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...
# Optional: set to 1 to let the pipeline call the Claude API without the
# extract-fixtures approval marker (used by CI after a human has approved cost).
# EXPENSE_ALLOW_API=1
